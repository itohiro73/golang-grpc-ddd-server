settings:
  legacy:
    force: false
    interval: 0s
schema:
  - name: server
    path: .
    commands:
      install:
        status: true
        method: go build ./cmd/server/
      run:
        status: true
        method: /go/bin/dlv
    args:
      - exec
      - ./server
      - --headless=true
      - --listen=:23450
      - --api-version=2
      - --accept-multiclient
    watcher:
      extensions:
        - go
      paths:
        - /
      ignore:
        paths:
          - .git
          - .realize
          - vendor
