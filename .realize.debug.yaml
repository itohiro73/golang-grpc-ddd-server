settings:
  legacy:
    force: false
    interval: 0s
schema:
  - name: app
    path: .
    commands:
      install:
        status: true
        method: go build -o golang-grpc-server ./cmd/grpc/main.go
      run:
        status: true
        method: /go/bin/dlv
    args:
      - exec
      - ./golang-grpc-server
      - --headless=true
      - --listen=:23450
      - --api-version=2
      - --accept-multiclient
    watcher:
      extensions:
        - go
      paths:
        - /
      ignore:
        paths:
          - .git
          - .realize
          - vendor
